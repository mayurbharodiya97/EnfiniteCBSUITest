import React from "react";
import DualPartTable from "./dualPartTable";

const DualTableCalc = ({ data }) => {
  const handleChange = () => {};
  return <DualPartTable /*data={data ?? []} handleChange={handleChange}*/ />;
};

export default DualTableCalc;
